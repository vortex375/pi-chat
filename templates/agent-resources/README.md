# Agent Resources

This directory defines backend-managed agent resources.

- `append-system-prompt.md` is appended to the backend agent's base system prompt.
- `skills/` contains custom skill directories. Each skill directory should include a `SKILL.md` file and any supporting scripts or references it needs.